<%= form_for(liff) do |f| %>
  <% if liff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(liff.errors.count, "error") %> prohibited this liff from being saved:</h2>

      <ul>
      <% liff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_field :body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
